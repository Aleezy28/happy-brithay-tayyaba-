Place your personal photos here, named exactly:

  pic1.jpg  — main memory photo (used in letter vault scratch card + scrapbook odd pages)
  pic2.jpg  — second memory photo (used in scrapbook even pages)
  pic3.jpg  — extra photo (used in scrapbook page 5+)

The app will automatically use these local files instead of the stock placeholder photos.
If a file is missing, it falls back to a placeholder image so nothing breaks.
